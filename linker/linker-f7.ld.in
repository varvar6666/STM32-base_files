/* ============================================================
 *  Unified safe STM32F7 linker script (FLASH + ITCM + DTCM + SRAM1 + SRAM2)
 *  - ITCM: 16KB (from .icf)
 *  - DTCM: size from map (64KB or 128KB)
 *  - SRAM2: fixed 16KB
 *  - SRAM1: computed = RAM_TOTAL - DTCM - SRAM2
 *  Compatible with STM32CubeMX-style startup
 * ============================================================ */

ENTRY(Reset_Handler)

/* ============================================================
 *  Memory layout (filled by CMake)
 * ============================================================ */
MEMORY
{
  FLASH (rx)  : ORIGIN = @FLASH_ORIGIN@,  LENGTH = @FLASH_LENGTH@

  /* Instruction TCM (CPU only) */
  ITCM  (rx)  : ORIGIN = @ITCM_ORIGIN@,   LENGTH = @ITCM_LENGTH@

  /* Data TCM (fast data, limited DMA visibility) */
  DTCM  (rwx) : ORIGIN = 0x20000000,      LENGTH = @DTCM_LENGTH@

  /* System SRAM split */
  SRAM1 (xrw) : ORIGIN = @SRAM1_ORIGIN@,  LENGTH = @SRAM1_LENGTH@
  SRAM2 (xrw) : ORIGIN = @SRAM2_ORIGIN@,  LENGTH = 0x4000
}

/* ============================================================
 *  Heap & stack (safe default: SRAM1)
 * ============================================================ */
_Min_Heap_Size  = @HEAP_SIZE@;
_Min_Stack_Size = @STACK_SIZE@;

/* End of stack in SRAM1 */
_estack = ORIGIN(SRAM1) + LENGTH(SRAM1);

/* ============================================================
 *  Sections
 * ============================================================ */
SECTIONS
{
  /* ----------------------------------------------------------
   *  Vector table in FLASH
   * ---------------------------------------------------------- */
  .isr_vector :
  {
    . = ALIGN(4);
    KEEP(*(.isr_vector))
    . = ALIGN(4);
  } > FLASH

  /* ----------------------------------------------------------
   *  Program code in FLASH (default)
   * ---------------------------------------------------------- */
  .text :
  {
    . = ALIGN(4);

    *(.text)
    *(.text*)
    *(.glue_7)
    *(.glue_7t)
    *(.eh_frame)

    KEEP(*(.init))
    KEEP(*(.fini))

    . = ALIGN(4);
    _etext = .;
  } > FLASH

  /* ----------------------------------------------------------
   *  Explicit ITCM code (opt-in)
   *  Use: __attribute__((section(".itcm_text")))
   * ---------------------------------------------------------- */
  _siitcm = LOADADDR(.itcm_text);

  .itcm_text : AT (ADDR(.text) + SIZEOF(.text))
  {
    . = ALIGN(4);
    __itcm_text_start__ = .;

    *(.itcm_text)
    *(.itcm_text*)

    . = ALIGN(4);
    __itcm_text_end__ = .;
  } > ITCM

  /* ----------------------------------------------------------
   *  Read-only data
   * ---------------------------------------------------------- */
  .rodata :
  {
    . = ALIGN(4);
    *(.rodata)
    *(.rodata*)
    . = ALIGN(4);
  } > FLASH

  /* ----------------------------------------------------------
   *  ARM exception tables (C++)
   * ---------------------------------------------------------- */
  .ARM.extab :
  {
    . = ALIGN(4);
    *(.ARM.extab* .gnu.linkonce.armextab.*)
    . = ALIGN(4);
  } > FLASH

  .ARM.exidx :
  {
    . = ALIGN(4);
    __exidx_start = .;
    *(.ARM.exidx*)
    __exidx_end = .;
    . = ALIGN(4);
  } > FLASH

  /* ----------------------------------------------------------
   *  C++ ctors/dtors
   * ---------------------------------------------------------- */
  .preinit_array :
  {
    . = ALIGN(4);
    PROVIDE_HIDDEN(__preinit_array_start = .);
    KEEP(*(.preinit_array*))
    PROVIDE_HIDDEN(__preinit_array_end = .);
    . = ALIGN(4);
  } > FLASH

  .init_array :
  {
    . = ALIGN(4);
    PROVIDE_HIDDEN(__init_array_start = .);
    KEEP(*(SORT(.init_array.*)))
    KEEP(*(.init_array*))
    PROVIDE_HIDDEN(__init_array_end = .);
    . = ALIGN(4);
  } > FLASH

  .fini_array :
  {
    . = ALIGN(4);
    PROVIDE_HIDDEN(__fini_array_start = .);
    KEEP(*(SORT(.fini_array.*)))
    KEEP(*(.fini_array*))
    PROVIDE_HIDDEN(__fini_array_end = .);
    . = ALIGN(4);
  } > FLASH

  /* ----------------------------------------------------------
   *  Initialized data in SRAM1 (safe DMA-visible default)
   * ---------------------------------------------------------- */
  _sidata = LOADADDR(.data);

  .data : AT (LOADADDR(.itcm_text) + SIZEOF(.itcm_text))
  {
    . = ALIGN(4);
    _sdata = .;
    __data_start__ = _sdata;

    *(.data)
    *(.data*)
    *(.RamFunc)
    *(.RamFunc*)

    . = ALIGN(4);
    _edata = .;
    __data_end__ = _edata;
  } > SRAM1

  /* ----------------------------------------------------------
   *  Explicit fast data in DTCM (opt-in)
   *  Use: __attribute__((section(".dtcm_data")))
   * ---------------------------------------------------------- */
  _sidtcm = LOADADDR(.dtcm_data);

  .dtcm_data : AT (LOADADDR(.data) + SIZEOF(.data))
  {
    . = ALIGN(4);
    __dtcm_data_start__ = .;

    *(.dtcm_data)
    *(.dtcm_data*)

    . = ALIGN(4);
    __dtcm_data_end__ = .;
  } > DTCM

  /* ----------------------------------------------------------
   *  Uninitialized data in SRAM1
   * ---------------------------------------------------------- */
  .bss :
  {
    . = ALIGN(4);
    _sbss = .;
    __bss_start__ = _sbss;

    *(.bss)
    *(.bss*)
    *(COMMON)

    . = ALIGN(4);
    _ebss = .;
    __bss_end__ = _ebss;
  } > SRAM1

  /* ----------------------------------------------------------
   *  Explicit uninitialized fast data in DTCM (opt-in)
   *  Use: __attribute__((section(".dtcm_bss")))
   * ---------------------------------------------------------- */
  .dtcm_bss (NOLOAD) :
  {
    . = ALIGN(4);
    __dtcm_bss_start__ = .;

    *(.dtcm_bss)
    *(.dtcm_bss*)

    . = ALIGN(4);
    __dtcm_bss_end__ = .;
  } > DTCM

  /* ----------------------------------------------------------
   *  Optional SRAM2 region usage (opt-in)
   *  Use: __attribute__((section(".sram2_bss")))
   * ---------------------------------------------------------- */
  .sram2_bss (NOLOAD) :
  {
    . = ALIGN(4);
    __sram2_bss_start__ = .;

    *(.sram2_bss)
    *(.sram2_bss*)

    . = ALIGN(4);
    __sram2_bss_end__ = .;
  } > SRAM2

  /* ----------------------------------------------------------
   *  Heap and stack in SRAM1
   * ---------------------------------------------------------- */
  ._user_heap_stack :
  {
    . = ALIGN(8);
    PROVIDE(end = .);
    PROVIDE(_end = .);

    . = . + _Min_Heap_Size;
    . = . + _Min_Stack_Size;

    . = ALIGN(8);
  } > SRAM1

  /* ----------------------------------------------------------
   *  Discard unused standard libraries
   * ---------------------------------------------------------- */
  /DISCARD/ :
  {
    libc.a ( * )
    libm.a ( * )
    libgcc.a ( * )
  }

  .ARM.attributes 0 : { *(.ARM.attributes) }
}
